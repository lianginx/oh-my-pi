不想再打 "keep going"？直接发一个 `.` 就行
你可以用 /btw 问一个旁路问题
用 /tan 把当前对话转成一个后台代理
Ctrl+D 可以退出，草稿会自动保存！
用 `omp stats` 看看你最常"虐待"哪个模型
试试任务隔离，创建 CoW 工作树
需要便宜的嵌套模型调用？用 `completion(x...)`。有一大批任务？让 clanker 用它！
代码一团糟？用 /omfg 抱怨一下
你知道吗？kitty/tmux/cmux/zellij/wezterm 的每个分屏都有自己的会话——`omp -c` 会恢复对应那个
消息里写上 `ultrathink` 可要求更深入的多步推理——输入时看它发出彩虹光
消息里说 `orchestrate` 可用并行子代理推进多阶段任务——输入时看它发光
消息里说 `workflowz` 可在 eval 中用并行子代理推进任务——输入时看它发光
同一提供商多登几个账号——再 `/login` 一次——omp 会自动在它们之间负载均衡
运行一次 `omp auth-broker serve`，每台机器都能在线拉取令牌——刷新密钥永不离机；`omp auth-gateway` 作为即插即用代理，任何 OpenAI 兼容客户端都能接入
按 alt+p（或 /switch）切换提供商，ctrl+p 循环角色模型 smol -> slow 等
按 ctrl+r 搜索历史提示词，复用之前的消息
模型总是用错工具时，`/force read` 把下一回合锁定到指定工具
`/copy code` 把最后一个代码块复制到剪贴板——`/copy cmd` 复制最后一条 shell/python 命令
`/shake` 把沉重的工具结果从上下文剥离以回收 token，无需完整 /compact——`/shake images` 只丢弃图片
实时结对：`/collab` 通过端到端加密的中继链接分享会话——队友运行 `/join <链接>` 即可在自己的 omp 里观看工具调用流并给代理发提示
按 ← ← 深入运行中或已完成的代理，查看其工具调用和记录
撞上 Codex 限流？`/usage reset` 消耗一次保存的重置额度立即恢复配额
模型没有原生工具调用？推理提供商解析不好？`PI_DIALECT=glm|kimi|anthropic…` 在本地替它搞定！
开启 `/advisor`，挂一个第二模型审阅每一回合并悄悄注入建议
试试用 -> 开头写提示，并写成列表（1. 做 X，2. 做 Y）
按 shift+tab 循环切换推理强度档位
